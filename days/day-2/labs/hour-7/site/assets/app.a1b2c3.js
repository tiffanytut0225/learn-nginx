const message = 'This hashed JavaScript asset is long enough to demonstrate gzip compression.';
console.log(message);
console.log('Its filename changes whenever its content changes, so immutable caching is safe.');
